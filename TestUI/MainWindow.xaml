<Window x:Class="TestUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:kld ="clr-namespace:QuestionareEntityFramework;assembly=QuestionareEntityFramework"      
        xmlns:local="clr-namespace:TestUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="864" Width="1079">
    <Grid RenderTransformOrigin="0.5,0.5" Margin="0,0,0,36" HorizontalAlignment="Left" Width="1072">

        <TreeView x:Name="treeView" HorizontalAlignment="Left"  Margin="10,10,0,0" VerticalAlignment="Top" >
            <TreeView.Resources>

                <HierarchicalDataTemplate DataType="{x:Type kld:Chapter}" ItemsSource="{Binding Path=Variants}">
                    <Border BorderBrush="RoyalBlue" Background="RoyalBlue" BorderThickness="1" CornerRadius="2" Margin="2" Padding="2" >
                        <StackPanel Orientation="Horizontal" >
                            <TextBlock  Text="{Binding Path=Text}" FontWeight="Bold" Foreground="White"></TextBlock>
                        </StackPanel>
                    </Border>
                </HierarchicalDataTemplate>

                <HierarchicalDataTemplate DataType="{x:Type kld:Variant}">
                    <Border Background="LightBlue" CornerRadius="2" Margin="1" >
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                                <ColumnDefinition Width="26"></ColumnDefinition>
                            </Grid.ColumnDefinitions>

                            <TextBlock Margin="2"  Text="{Binding Path=Text}"></TextBlock>

                            <Label Grid.Column="1" Content="OOPOPO"></Label>
                        </Grid>
                    </Border>
                </HierarchicalDataTemplate>

            </TreeView.Resources>
        </TreeView>



    </Grid>
</Window>
